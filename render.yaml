services:
  - type: web
    name: myntra-rating-tool
    env: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: streamlit run app.py --server.address=0.0.0.0 --server.port=$PORT